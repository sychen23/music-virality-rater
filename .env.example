BETTER_AUTH_SECRET="your-secret-key"
BETTER_AUTH_URL="your-app-url"

GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Neon Local (Docker) — run `docker compose up -d` for local dev
NEON_API_KEY="your-neon-api-key"
NEON_PROJECT_ID="your-neon-project-id"
# Optional: set to create ephemeral branches from a specific parent
# PARENT_BRANCH_ID="your-parent-branch-id"

# Local dev: points at Neon Local proxy (docker compose up -d)
# Production: use your Neon cloud connection string
DATABASE_URL="postgres://neon:npg@localhost:5434/neondb"

CLEANUP_SECRET="your-cleanup-secret"

# Vercel Blob — create a store in the Vercel dashboard under Storage > Blob
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."

# Anthropic — AI-powered insights (claude-opus-4-6)
ANTHROPIC_API_KEY="sk-ant-..."